name: blushy_life_app
description: "A new Flutter project for blushy.life."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: '>=3.10.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  google_fonts: ^8.2.0
  dio: ^5.8.0
  http: ^1.2.2
  url_launcher: ^6.3.1
  google_sign_in: ^6.2.1
  provider: ^6.1.2
  confetti: ^0.7.0
  share_plus: ^10.0.0
  shared_preferences: ^2.2.0
  path_provider: ^2.1.2
  web_socket_channel: ^3.0.3

  crypto: ^3.0.6
  web: ^1.1.0
  record: ^6.0.0
  just_audio: ^0.9.40
  file_picker: ^8.1.2
  http_parser: ^4.0.2
  lottie: ^3.5.1


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  # Drives the midnight rollover timer on a fake clock, so the test does
  # not have to wait until midnight to know whether it fires.
  fake_async: ^1.3.0

flutter:

  uses-material-design: true
  generate: true

  assets:
    - assets/
    # Optional check-in card animations. Empty by default: the cards draw
    # their own scenes and use a file here only if one is added.
    - assets/lottie/
    - assets/color/flowers/
    - assets/color/bush/
    - assets/mono/flowers/
    - assets/mono/bush/

  # The layouts asked for Georgia and Courier and shipped neither, so on Android
  # both fell back to Roboto and the editorial serif was lost. Neither is
  # redistributable, so these are the open-licensed metric-compatible
  # equivalents -- same widths, so nothing had to be re-tuned. Registered under
  # the names the code already uses, so no call site changes.
  # See assets/fonts/README.md and OFL.txt.
  fonts:
    - family: Georgia
      fonts:
        - asset: assets/fonts/Gelasio-Regular.ttf
        - asset: assets/fonts/Gelasio-Italic.ttf
          style: italic
        - asset: assets/fonts/Gelasio-Bold.ttf
          weight: 700
    - family: Courier
      fonts:
        - asset: assets/fonts/CourierPrime-Regular.ttf
        - asset: assets/fonts/CourierPrime-Bold.ttf
          weight: 700
    - family: InstrumentSerif
      fonts:
        - asset: assets/fonts/InstrumentSerif-Regular.ttf
        - asset: assets/fonts/InstrumentSerif-Italic.ttf
          style: italic
    - family: CormorantGaramond
      fonts:
        - asset: assets/fonts/CormorantGaramond[wght].ttf
          weight: 400
        - asset: assets/fonts/CormorantGaramond[wght].ttf
          weight: 600
        - asset: assets/fonts/CormorantGaramond[wght].ttf
          weight: 700
        - asset: assets/fonts/CormorantGaramond[wght].ttf
          weight: 800
        - asset: assets/fonts/CormorantGaramond-Italic[wght].ttf
          style: italic
    - family: DMSerifDisplay
      fonts:
        - asset: assets/fonts/DMSerifDisplay-Regular.ttf
        - asset: assets/fonts/DMSerifDisplay-Italic.ttf
          style: italic
    - family: AdaHybrid
      fonts:
        - asset: assets/fonts/AdaHybrid-Bold.ttf
    - family: Inter
      fonts:
        - asset: assets/fonts/Inter[opsz,wght].ttf
        - asset: assets/fonts/Inter-Italic[opsz,wght].ttf
          style: italic
    - family: Manrope
      fonts:
        - asset: assets/fonts/Manrope-Regular.ttf
        - asset: assets/fonts/Manrope-Medium.ttf
          weight: 500
        - asset: assets/fonts/Manrope-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Manrope-Bold.ttf
          weight: 700



  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
