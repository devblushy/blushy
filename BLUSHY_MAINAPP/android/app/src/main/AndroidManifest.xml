<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- Voice notes and Sia voice input record audio on-device; the bytes are
         uploaded for transcription and the local file is deleted after. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <!-- Android 13 (API 33) and above will not display a notification without
         this. The app registers a push token and shows a notification inbox, so
         without it those notifications were silently never delivered. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- No android:usesCleartextTraffic here. It was "true", which permits
         unencrypted HTTP for the whole app, needed only to reach a local
         backend during development, and a finding against a health app that
         handles cycle, symptom and journal data. Debug builds still get it,
         from android/app/src/debug/AndroidManifest.xml. -->
    <application
        android:label="Blushy"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep links. Hosts are listed explicitly: a bare
                 <data android:scheme="blushy"/> matches every blushy:// URI, so
                 any installed app could launch Blushy with an arbitrary one.
                 These three cover every link the app actually issues:
                   blushy://auth/email-verified, blushy://auth/swiggy
                   blushy://partner/sharing, blushy://partner/boutique
                   blushy://studio/capsules -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="blushy" android:host="auth" />
                <data android:scheme="blushy" android:host="partner" />
                <data android:scheme="blushy" android:host="studio" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
